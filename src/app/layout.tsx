import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Aurora Pulse Collective | Future-Ready Marketing Agency Blueprint",
  description:
    "Step-by-step strategy to build Aurora Pulse Collective — a hybrid storytelling and intelligence-led marketing agency for modern India.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
